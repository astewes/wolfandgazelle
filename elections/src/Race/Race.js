import React from 'react';
import './Race.css';

// onClick={props.click}
// onChange={props.changed} value={props.name}
// {props.name}, {props.age}, {props.children}

const race = () => {
  return (
    <div className="Race">
      <div className="dh">
        Race
      </div>
      <div className="db">
        Results
      </div>
    </div>
  )
}

export default race;
